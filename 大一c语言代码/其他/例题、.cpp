#include<stdio.h>
main()
{
	int a[5]={1,2,3,4,5};
	int *p=(int *)(&a+1);   /* 加了&代表二维指针,是p向后移动整个数组长的单位,即指向5后边的元素（为内存所分配）*/
	int *q=(int *)(a+1);    /* 是一维指针,即指向a[1]的值*/
	printf("%d,%d,%d",*(a+1),*(p-1),*(q-1));
}
